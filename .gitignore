# Environment — never commit API keys
.env
.env.*

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Generated game files (created at runtime)
games/

# All generated assets are committed so we never regenerate what already exists
# assets/backgrounds/  — committed
# assets/generated/    — committed

# macOS
.DS_Store
